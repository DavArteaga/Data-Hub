# Production env vars for the Vercel deployment.
# Copy to .env.production (or set in the Vercel dashboard) and edit.
#
# Must point to the public Render backend URL (https, no trailing slash):
VITE_API_URL=https://datacore-hub-api.onrender.com/api/v1
